const OUTPUT_DIR = "next-banner";
const CONFIG_FILE = "next-banner.json";
const LAYOUT_DIR = "_banner";

const DEFAULT_LAYOUT = "default";

export { OUTPUT_DIR, CONFIG_FILE, LAYOUT_DIR, DEFAULT_LAYOUT };
