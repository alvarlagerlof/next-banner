{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "version": "cp README.md packages/core && cp LICENSE packages/core",
    "release": "turbo run release && np"
  },
  "devDependencies": {
    "np": "*",
    "turbo": "^1.3.1"
  },
  "packageManager": "yarn@3.2.1",
  "version": "1.2.1"
}
